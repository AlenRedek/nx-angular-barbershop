import { AppointmentFormComponent } from './appointment-form.component';

describe('AppointmentFormComponent', () => {
  let component: AppointmentFormComponent;

  beforeEach(() => {
    component = new AppointmentFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
